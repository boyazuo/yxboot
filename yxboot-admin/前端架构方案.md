# 前端架构方案

---

## 📋 目录

- [一、架构设计理念](#一架构设计理念)
- [二、整体架构设计](#二整体架构设计)
- [三、技术栈选型](#三技术栈选型)
- [四、目录结构设计](#四目录结构设计)
- [五、核心模块详解](#五核心模块详解)
- [六、工程化配置](#六工程化配置)
- [七、开发规范](#七开发规范)
- [八、使用示例](#八使用示例)
- [九、性能优化策略](#九性能优化策略)
- [十、最佳实践](#十最佳实践)

---

## 一、架构设计理念

### 1.1 核心原则

#### 🎯 简洁至上
- **单一职责**：每个模块只做一件事，做好一件事
- **最小包数量**：优先合并，避免过度拆分
- **清晰命名**：见名知意，避免模糊概念

#### 🔄 渐进增强
- **核心最小化**：核心包含框架必需功能
- **插件可选化**：非必需功能以插件形式提供
- **按需加载**：通过子路径导出支持 Tree-shaking

#### 🏗️ 分层清晰
- **只分两层**：核心层 + 插件层
- **单向依赖**：插件依赖核心，核心不依赖插件
- **零循环依赖**：通过依赖扫描工具保证

#### 🎨 UI 无关
- **适配器模式**：支持多种 UI 组件库
- **核心与 UI 分离**：核心逻辑不依赖具体 UI 库
- **统一接口**：通过适配器提供统一的组件接口

### 1.2 设计目标

| 目标 | 指标 |
|------|------|
| **易理解** | 新人 1 天内理解整体架构 |
| **易维护** | 修改一个功能平均影响 < 3 个文件 |
| **易扩展** | 新增 UI 库仅需添加适配器 |
| **高性能** | 首屏加载 < 2s，构建时间 < 30s |
| **高质量** | TypeScript 覆盖率 100%，单测覆盖率 > 80% |

---

## 二、整体架构设计

### 2.1 架构分层

```
┌─────────────────────────────────────────────────────────┐
│                     应用层 (apps/)                       │
│  web-antd │ web-element │ web-naive │ admin-mobile      │
└────────────────────────┬────────────────────────────────┘
                         │ 依赖
┌────────────────────────┴────────────────────────────────┐
│                   插件层 (plugins/)                      │
│  echarts │ vxe-table │ workflow │ ai-assistant          │
└────────────────────────┬────────────────────────────────┘
                         │ 依赖
┌────────────────────────┴────────────────────────────────┐
│                   核心层 (@core/)                        │
│  base │ ui │ runtime │ adapters                         │
└─────────────────────────────────────────────────────────┘
```

### 2.2 包结构总览

```
项目根目录/
├── packages/
│   ├── @core/                    # 🔷 核心框架（1 个包）
│   └── plugins/                  # 🔌 可选插件（多个独立包）
├── apps/                         # 📱 应用实例
├── tooling/                      # 🛠️ 工具和配置
└── docs/                         # 📚 文档站点
```

---

## 三、技术栈选型

### 3.1 核心技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| **Vue** | 3.5+ | 前端框架 |
| **TypeScript** | 5.9+ | 类型系统 |
| **Vite** | 7.x | 构建工具 |
| **Pinia** | 3.x | 状态管理 |
| **Vue Router** | 4.x | 路由管理 |
| **VueUse** | 14.x | 组合式函数库 |
| **Axios** | 1.x | HTTP 客户端 |
| **Vue I18n** | 11.x | 国际化 |
| **Zod** | 3.x | 运行时类型校验 |
| **Tailwind CSS** | 3.x | 样式框架 |

### 3.2 工程化工具

| 工具 | 用途 |
|------|------|
| **pnpm** | 包管理器（workspace + catalog） |
| **Turbo** | Monorepo 构建编排 |
| **Biome** | 代码检查 + 格式化（替代 ESLint + Prettier + Stylelint） |
| **Lefthook** | Git hooks 管理 |
| **Vitest** | 单元测试 |
| **Playwright** | E2E 测试 |
| **Changesets** | 版本管理 |

> 💡 **为什么选择 Biome？**
> - ⚡ **极致性能**：比 ESLint + Prettier 快 10-100 倍（Rust 编写）
> - 🎯 **统一工具**：一个工具同时处理 lint 和 format
> - 🚀 **零配置**：开箱即用，合理的默认配置
> - 🔧 **更好的错误提示**：清晰的诊断信息
> - 📦 **体积小**：单个二进制文件，无需大量依赖

### 3.3 可选 UI 库支持

- Ant Design Vue 4.x
- Element Plus 2.x
- Naive UI 2.x
- Arco Design Vue
- TDesign Vue Next

---

## 四、目录结构设计

### 4.1 完整目录树

```
yxboot-admin/
├── apps/                         # 📱 应用层
│   ├── web-antd/                 # Ant Design Vue 应用
│   │   ├── public/
│   │   ├── src/
│   │   │   ├── assets/           # 静态资源
│   │   │   ├── adapter/          # UI 适配器
│   │   │   │   ├── components.ts # 组件适配
│   │   │   │   └── form.ts       # 表单适配
│   │   │   ├── api/              # API 接口
│   │   │   │   ├── modules/      # 按模块划分
│   │   │   │   └── request.ts    # 请求客户端
│   │   │   ├── layouts/          # 布局
│   │   │   │   ├── default.vue
│   │   │   │   └── auth.vue
│   │   │   ├── router/           # 路由
│   │   │   │   ├── routes/
│   │   │   │   ├── guards.ts
│   │   │   │   └── index.ts
│   │   │   ├── store/            # 应用级状态
│   │   │   │   └── auth.ts
│   │   │   ├── views/            # 页面视图
│   │   │   │   ├── dashboard/
│   │   │   │   ├── system/
│   │   │   │   └── auth/
│   │   │   ├── App.vue
│   │   │   ├── main.ts
│   │   │   └── env.d.ts
│   │   ├── index.html
│   │   ├── package.json
│   │   ├── tsconfig.json
│   │   └── vite.config.ts
│   ├── web-element/              # Element Plus 应用
│   ├── web-naive/                # Naive UI 应用
│   └── admin-mobile/             # 移动端应用
├── packages/                     # 📦 核心包和插件
│   ├── @core/                    # 🔷 核心框架
│   │   ├── src/
│   │   │   ├── base/             # 基础模块
│   │   │   │   ├── constants/    # 常量定义
│   │   │   │   ├── types/        # 类型定义
│   │   │   │   └── utils/        # 工具函数
│   │   │   ├── ui/               # UI 组件库（UI 无关）
│   │   │   │   ├── form/         # 表单组件
│   │   │   │   ├── layout/       # 布局组件
│   │   │   │   ├── menu/         # 菜单组件
│   │   │   │   ├── modal/        # 弹窗组件
│   │   │   │   ├── table/        # 表格组件
│   │   │   │   ├── tabs/         # 标签页组件
│   │   │   │   └── index.ts
│   │   │   ├── runtime/          # 运行时核心
│   │   │   │   ├── access/       # 权限控制
│   │   │   │   ├── request/      # HTTP 请求
│   │   │   │   ├── router/       # 路由增强
│   │   │   │   ├── store/        # 状态管理
│   │   │   │   ├── i18n/         # 国际化
│   │   │   │   ├── theme/        # 主题系统
│   │   │   │   ├── cache/        # 缓存管理
│   │   │   │   └── logger/       # 日志系统
│   │   │   ├── composables/      # 组合式函数
│   │   │   │   ├── use-app.ts
│   │   │   │   ├── use-auth.ts
│   │   │   │   ├── use-theme.ts
│   │   │   │   └── index.ts
│   │   │   ├── directives/       # 指令
│   │   │   │   ├── loading.ts
│   │   │   │   ├── permission.ts
│   │   │   │   └── index.ts
│   │   │   ├── styles/           # 样式
│   │   │   │   ├── base.css
│   │   │   │   ├── variables.css
│   │   │   │   └── index.css
│   │   │   └── index.ts          # 主入口
│   │   ├── package.json
│   │   ├── tsconfig.json
│   │   └── README.md
│   └── plugins/                  # 🔌 可选插件
│       ├── echarts/              # ECharts 图表
│       │   ├── src/
│       │   └── package.json
│       ├── vxe-table/            # VXE Table
│       │   ├── src/
│       │   └── package.json
│       ├── workflow/             # 工作流引擎
│       │   ├── src/
│       │   └── package.json
│       └── ai-assistant/         # AI 助手
│           ├── src/
│           └── package.json
├── tooling/                      # 🛠️ 工具和配置
│   ├── biome/                    # Biome 配置（可选，也可直接用根目录 biome.json）
│   │   └── package.json
│   ├── tsconfig/                 # TypeScript 配置
│   │   ├── base.json
│   │   ├── app.json
│   │   ├── lib.json
│   │   └── package.json
│   ├── vite/                     # Vite 配置
│   │   ├── src/
│   │   └── package.json
│   └── tailwind/                 # Tailwind 配置
│       ├── index.js
│       └── package.json
├── docs/                         # 📚 文档站点
│   ├── .vitepress/
│   ├── guide/
│   ├── api/
│   └── package.json
├── scripts/                      # 📜 脚本
│   ├── build.ts
│   ├── dev.ts
│   └── clean.ts
├── .changeset/                   # Changesets 配置
├── .editorconfig                 # 编辑器配置
├── .gitignore
├── .npmrc                        # pnpm 配置
├── biome.json                    # Biome 配置（代码检查和格式化）
├── lefthook.yml                  # Git hooks
├── package.json                  # 根配置
├── pnpm-workspace.yaml           # pnpm workspace
├── turbo.json                    # Turbo 配置
├── tsconfig.json                 # TypeScript 根配置
└── README.md
```

### 4.2 核心包内部结构详解

#### 📁 `packages/@core/src/base/` - 基础模块

```
base/
├── constants/                    # 常量定义
│   ├── app.ts                    # 应用常量
│   ├── cache.ts                  # 缓存键名
│   ├── http.ts                   # HTTP 常量
│   └── index.ts
├── types/                        # 类型定义
│   ├── app.ts                    # 应用类型
│   ├── auth.ts                   # 认证类型
│   ├── router.ts                 # 路由类型
│   ├── store.ts                  # Store 类型
│   ├── http.ts                   # HTTP 类型
│   ├── component.ts              # 组件类型
│   └── index.ts
└── utils/                        # 工具函数
    ├── array.ts                  # 数组工具
    ├── object.ts                 # 对象工具
    ├── string.ts                 # 字符串工具
    ├── date.ts                   # 日期工具
    ├── tree.ts                   # 树形结构工具
    ├── storage.ts                # 存储工具
    ├── crypto.ts                 # 加密工具
    ├── url.ts                    # URL 工具
    ├── validator.ts              # 验证器
    └── index.ts
```

#### 📁 `packages/@core/src/ui/` - UI 组件库

```
ui/
├── form/                         # 表单组件
│   ├── Form.vue
│   ├── FormItem.vue
│   ├── FormRender.vue            # 动态表单渲染器
│   ├── hooks.ts
│   ├── types.ts
│   └── index.ts
├── layout/                       # 布局组件
│   ├── Layout.vue
│   ├── Header.vue
│   ├── Sidebar.vue
│   ├── Content.vue
│   ├── Footer.vue
│   └── index.ts
├── menu/                         # 菜单组件
│   ├── Menu.vue
│   ├── MenuItem.vue
│   ├── SubMenu.vue
│   ├── hooks.ts
│   └── index.ts
├── modal/                        # 弹窗组件
│   ├── Modal.vue
│   ├── Drawer.vue
│   ├── hooks.ts                  # useModal, useDrawer
│   └── index.ts
├── table/                        # 表格组件
│   ├── Table.vue
│   ├── TableAction.vue
│   ├── hooks.ts
│   └── index.ts
├── tabs/                         # 标签页组件
│   ├── Tabs.vue
│   ├── TabPane.vue
│   ├── hooks.ts
│   └── index.ts
└── index.ts                      # 统一导出
```

#### 📁 `packages/@core/src/runtime/` - 运行时核心

```
runtime/
├── access/                       # 权限控制
│   ├── index.ts
│   ├── guard.ts                  # 路由守卫
│   ├── directive.ts              # 权限指令
│   ├── checker.ts                # 权限检查器
│   └── types.ts
├── request/                      # HTTP 请求
│   ├── index.ts
│   ├── client.ts                 # 请求客户端
│   ├── interceptors.ts           # 拦截器
│   ├── retry.ts                  # 重试逻辑
│   ├── cancel.ts                 # 请求取消
│   ├── upload.ts                 # 文件上传
│   ├── download.ts               # 文件下载
│   └── types.ts
├── router/                       # 路由增强
│   ├── index.ts
│   ├── generator.ts              # 路由生成器
│   ├── matcher.ts                # 路由匹配器
│   ├── transformer.ts            # 路由转换器
│   └── types.ts
├── store/                        # 状态管理
│   ├── index.ts
│   ├── modules/                  # Store 模块
│   │   ├── app.ts                # 应用状态
│   │   ├── user.ts               # 用户状态
│   │   ├── auth.ts               # 认证状态
│   │   ├── permission.ts         # 权限状态
│   │   ├── tabs.ts               # 标签页状态
│   │   └── theme.ts              # 主题状态
│   └── types.ts
├── i18n/                         # 国际化
│   ├── index.ts
│   ├── setup.ts                  # 初始化
│   ├── loader.ts                 # 语言包加载器
│   └── types.ts
├── theme/                        # 主题系统
│   ├── index.ts
│   ├── preset.ts                 # 预设主题
│   ├── generator.ts              # 主题生成器
│   ├── applier.ts                # 主题应用器
│   └── types.ts
├── cache/                        # 缓存管理
│   ├── index.ts
│   ├── memory.ts                 # 内存缓存
│   ├── storage.ts                # 持久化缓存
│   └── types.ts
└── logger/                       # 日志系统
    ├── index.ts
    ├── console.ts                # 控制台日志
    ├── remote.ts                 # 远程日志
    └── types.ts
```

---

## 五、核心模块详解

### 5.1 Base 模块 - 基础能力

#### 职责
- 提供框架无关的基础工具函数
- 定义 TypeScript 类型
- 定义全局常量

#### 设计原则
- ✅ 零依赖（除基础库如 lodash-es、dayjs）
- ✅ 纯函数优先
- ✅ 完整的类型定义
- ✅ 100% 单元测试覆盖

#### 导出示例

```typescript
// packages/@core/src/base/index.ts
export * from './constants';
export * from './types';
export * from './utils';

// 使用
import { formatDate, isValidEmail, CACHE_KEYS } from '@core/base';
```

---

### 5.2 UI 模块 - UI 组件库

#### 职责
- 提供 UI 无关的组件抽象
- 定义组件接口和 Props
- 实现组件核心逻辑

#### 设计原则
- ✅ **UI 库无关**：不依赖任何具体 UI 库
- ✅ **适配器模式**：通过适配器接入不同 UI 库
- ✅ **组合式 API**：提供 Hooks 供业务使用
- ✅ **高度可定制**：通过 Props 和 Slots 实现

#### 核心组件

##### 📋 Form 表单组件

**特性：**
- 动态表单渲染（JSON Schema）
- 自动校验（基于 Zod）
- 联动逻辑支持
- 条件显示/隐藏
- 自定义组件支持

**使用示例：**

```vue
<template>
  <VbenForm
    :schema="schema"
    :model="formData"
    @submit="handleSubmit"
  />
</template>

<script setup lang="ts">
import { VbenForm } from '@core/ui/form';
import { z } from 'zod';

const schema = [
  {
    field: 'username',
    label: '用户名',
    component: 'Input',
    rules: z.string().min(3).max(20),
  },
  {
    field: 'email',
    label: '邮箱',
    component: 'Input',
    rules: z.string().email(),
  },
];
</script>
```

##### 🗂️ Table 表格组件

**特性：**
- 虚拟滚动（支持大数据量）
- 服务端分页/排序/筛选
- 行选择（单选/多选）
- 列配置（显示/隐藏、拖拽排序）
- 导出功能（Excel/CSV）

##### 📑 Tabs 标签页组件

**特性：**
- 多标签页管理
- 拖拽排序
- 右键菜单（关闭、关闭其他、关闭所有）
- 持久化（刷新保持）
- 标签页缓存（keep-alive）

---

### 5.3 Runtime 模块 - 运行时核心

#### 5.3.1 Access - 权限控制

**功能：**
- 路由权限控制
- 按钮权限控制
- 数据权限控制
- 动态路由生成

**权限模式：**
```typescript
export type AccessMode =
  | 'frontend'  // 前端路由（路由表在前端定义，根据权限过滤）
  | 'backend'   // 后端路由（从后端获取路由配置）
  | 'mixed';    // 混合模式（前端基础路由 + 后端动态路由）
```

**使用示例：**

```typescript
// 1. 路由守卫
import { createAccessGuard } from '@core/runtime/access';

const router = createRouter({...});
createAccessGuard(router, {
  mode: 'backend',
  loginPath: '/login',
  defaultHomePath: '/dashboard',
});

// 2. 权限指令
<template>
  <button v-permission="'user:delete'">删除</button>
</template>

// 3. 权限检查
import { useAccess } from '@core/runtime/access';

const { hasPermission, hasRole } = useAccess();

if (hasPermission('user:edit')) {
  // 执行编辑操作
}
```

#### 5.3.2 Request - HTTP 请求

**特性：**
- 请求/响应拦截器
- Token 自动刷新
- 请求重试
- 请求取消
- 请求缓存
- 文件上传（支持进度）
- 文件下载
- SSE（Server-Sent Events）支持

**使用示例：**

```typescript
import { createHttpClient } from '@core/runtime/request';

// 创建客户端
const http = createHttpClient({
  baseURL: '/api',
  timeout: 10000,
  retry: 3,
});

// 请求拦截器
http.interceptors.request.use((config) => {
  const token = getToken();
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// 响应拦截器
http.interceptors.response.use(
  (response) => response.data,
  (error) => {
    if (error.response?.status === 401) {
      // 跳转登录
    }
    return Promise.reject(error);
  }
);

// 使用
const data = await http.get('/users');
const result = await http.post('/users', { name: 'John' });
```

#### 5.3.3 Store - 状态管理

**Store 模块：**

```typescript
// App Store - 应用全局状态
interface AppState {
  collapsed: boolean;           // 侧边栏折叠
  mobile: boolean;              // 是否移动端
  locale: string;               // 当前语言
  theme: 'light' | 'dark';      // 主题模式
  layoutMode: LayoutMode;       // 布局模式
}

// User Store - 用户状态
interface UserState {
  userInfo: UserInfo | null;    // 用户信息
  roles: string[];              // 角色列表
  permissions: string[];        // 权限列表
}

// Auth Store - 认证状态
interface AuthState {
  token: string | null;         // 访问令牌
  refreshToken: string | null;  // 刷新令牌
  loginTime: number | null;     // 登录时间
  expireTime: number | null;    // 过期时间
}

// Tabs Store - 标签页状态
interface TabsState {
  tabs: Tab[];                  // 标签页列表
  activeTab: string;            // 当前激活标签
  cacheList: string[];          // 缓存列表
}
```

#### 5.3.4 Theme - 主题系统

**特性：**
- 明暗主题切换
- 自定义主题色
- 预设主题库
- CSS 变量动态更新
- 主题持久化

**使用示例：**

```typescript
import { useTheme } from '@core/runtime/theme';

const { theme, setTheme, setPrimaryColor } = useTheme();

// 切换主题
setTheme('dark');

// 设置主题色
setPrimaryColor('#1890ff');

// 应用预设主题
import { presetThemes } from '@core/runtime/theme';
setTheme(presetThemes.blue);
```

---

### 5.4 Composables 模块 - 组合式函数

#### 核心 Composables

```typescript
// useApp - 应用配置
export function useApp() {
  return {
    collapsed,
    toggleCollapsed,
    isMobile,
    layoutMode,
    setLayoutMode,
  };
}

// useAuth - 认证管理
export function useAuth() {
  return {
    isAuthenticated,
    login,
    logout,
    refreshToken,
  };
}

// usePermission - 权限判断
export function usePermission() {
  return {
    hasPermission,
    hasRole,
    hasAnyPermission,
    hasAllPermissions,
  };
}

// useTable - 表格管理
export function useTable(options) {
  return {
    data,
    loading,
    pagination,
    load,
    reload,
    search,
    reset,
  };
}

// useForm - 表单管理
export function useForm(schema) {
  return {
    formRef,
    formData,
    validate,
    reset,
    setFieldValue,
    getFieldValue,
  };
}

// useModal - 弹窗管理
export function useModal() {
  return {
    open,
    close,
    confirm,
    register,
  };
}

// useTabs - 标签页管理
export function useTabs() {
  return {
    tabs,
    activeTab,
    addTab,
    removeTab,
    refreshTab,
    closeOtherTabs,
    closeAllTabs,
  };
}
```

---

### 5.5 Directives 模块 - 自定义指令

```typescript
// v-loading - 加载状态
<div v-loading="loading">内容</div>

// v-permission - 权限控制
<button v-permission="'user:delete'">删除</button>

// v-role - 角色控制
<div v-role="'admin'">管理员可见</div>

// v-debounce - 防抖
<button v-debounce:click="handleClick">点击</button>

// v-throttle - 节流
<button v-throttle:click="handleClick">点击</button>

// v-copy - 一键复制
<span v-copy="text">复制内容</span>

// v-ellipsis - 文本省略
<div v-ellipsis:2>长文本内容</div>
```

---

## 六、工程化配置

### 6.1 Monorepo 配置

#### pnpm-workspace.yaml

```yaml
packages:
  - 'packages/@core'
  - 'packages/plugins/*'
  - 'apps/*'
  - 'tooling/*'
  - 'docs'

# 依赖版本统一管理
catalog:
  vue: ^3.5.27
  typescript: ^5.9.3
  vite: ^7.3.1
  pinia: ^3.0.4
  vue-router: ^4.6.4
  axios: ^1.13.4
  zod: ^3.25.76
  tailwindcss: ^3.4.19
  # ... 更多依赖
```

#### turbo.json

```json
{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [
    "pnpm-lock.yaml",
    "**/.env.*local",
    "**/tsconfig*.json"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build"]
    }
  }
}
```

### 6.2 TypeScript 配置

#### 基础配置

```json
// tooling/tsconfig/base.json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "preserve",
    "moduleResolution": "Bundler",
    "resolveJsonModule": true,
    "allowImportingTsExtensions": true,
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true
  }
}
```

#### 应用配置

```json
// tooling/tsconfig/app.json
{
  "extends": "./base.json",
  "compilerOptions": {
    "composite": false,
    "declaration": false,
    "declarationMap": false,
    "types": ["vite/client", "node"]
  }
}
```

### 6.3 Biome 配置

#### 安装

```bash
pnpm add -Dw @biomejs/biome
```

#### 配置文件

```json
// biome.json（项目根目录）
{
  "$schema": "https://biomejs.dev/schemas/1.9.4/schema.json",
  "vcs": {
    "enabled": true,
    "clientKind": "git",
    "useIgnoreFile": true,
    "defaultBranch": "main"
  },
  "files": {
    "ignoreUnknown": false,
    "ignore": [
      "node_modules",
      "dist",
      "*.min.js",
      ".turbo",
      ".changeset"
    ],
    "include": ["**/*.js", "**/*.ts", "**/*.vue", "**/*.json"]
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineWidth": 100,
    "lineEnding": "lf"
  },
  "linter": {
    "enabled": true,
    "rules": {
      "recommended": true,
      "complexity": {
        "noExtraBooleanCast": "error",
        "noMultipleSpacesInRegularExpressionLiterals": "error",
        "noUselessCatch": "error",
        "noUselessConstructor": "error",
        "noUselessLabel": "error",
        "noUselessRename": "error",
        "noVoid": "error",
        "noWith": "error"
      },
      "correctness": {
        "noConstAssign": "error",
        "noConstantCondition": "error",
        "noEmptyCharacterClassInRegex": "error",
        "noEmptyPattern": "error",
        "noGlobalObjectCalls": "error",
        "noInnerDeclarations": "error",
        "noInvalidConstructorSuper": "error",
        "noNewSymbol": "error",
        "noNonoctalDecimalEscape": "error",
        "noPrecisionLoss": "error",
        "noSelfAssign": "error",
        "noSetterReturn": "error",
        "noSwitchDeclarations": "error",
        "noUndeclaredVariables": "error",
        "noUnreachable": "error",
        "noUnreachableSuper": "error",
        "noUnsafeFinally": "error",
        "noUnsafeOptionalChaining": "error",
        "noUnusedLabels": "error",
        "noUnusedVariables": "error",
        "useIsNan": "error",
        "useValidForDirection": "error",
        "useYield": "error"
      },
      "style": {
        "noCommaOperator": "error",
        "noNegationElse": "off",
        "noParameterAssign": "error",
        "noRestrictedGlobals": "error",
        "noVar": "error",
        "useConst": "error",
        "useExponentiationOperator": "error",
        "useTemplate": "error"
      },
      "suspicious": {
        "noAsyncPromiseExecutor": "error",
        "noCatchAssign": "error",
        "noClassAssign": "error",
        "noCompareNegZero": "error",
        "noControlCharactersInRegex": "error",
        "noDebugger": "error",
        "noDoubleEquals": "warn",
        "noDuplicateCase": "error",
        "noDuplicateClassMembers": "error",
        "noDuplicateObjectKeys": "error",
        "noDuplicateParameters": "error",
        "noEmptyBlockStatements": "error",
        "noExplicitAny": "warn",
        "noExtraNonNullAssertion": "error",
        "noFallthroughSwitchClause": "error",
        "noFunctionAssign": "error",
        "noGlobalAssign": "error",
        "noImportAssign": "error",
        "noMisleadingCharacterClass": "error",
        "noPrototypeBuiltins": "error",
        "noRedeclare": "error",
        "noShadowRestrictedNames": "error",
        "noUnsafeNegation": "error",
        "useGetterReturn": "error",
        "useValidTypeof": "error"
      }
    }
  },
  "javascript": {
    "formatter": {
      "quoteStyle": "single",
      "jsxQuoteStyle": "double",
      "quoteProperties": "asNeeded",
      "trailingCommas": "all",
      "semicolons": "always",
      "arrowParentheses": "always",
      "bracketSpacing": true,
      "bracketSameLine": false
    }
  },
  "json": {
    "formatter": {
      "enabled": true,
      "indentStyle": "space",
      "indentWidth": 2,
      "lineWidth": 100
    },
    "linter": {
      "enabled": true
    }
  },
  "css": {
    "formatter": {
      "enabled": true,
      "indentStyle": "space",
      "indentWidth": 2,
      "lineWidth": 100
    },
    "linter": {
      "enabled": true
    }
  },
  "overrides": [
    {
      "include": ["*.vue"],
      "linter": {
        "rules": {
          "correctness": {
            "noUndeclaredVariables": "off"
          }
        }
      }
    }
  ]
}
```

#### Vue 文件支持

Biome 原生不完全支持 `.vue` 文件，但可以配合使用：

```json
// package.json
{
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "lint:vue": "eslint --ext .vue src",
    "lint:all": "pnpm lint && pnpm lint:vue"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "eslint-plugin-vue": "^10.7.0"  // 仅用于 Vue 文件
  }
}
```

> 💡 **Vue 文件处理策略**：
> 
> **方案 1（推荐）：** Biome 处理 TS/JS，轻量级 ESLint 仅处理 .vue 文件
> ```bash
> # 检查所有代码
> biome check . && eslint "**/*.vue"
> ```
> 
> **方案 2：** 等待 Biome 完整支持 Vue（预计 2026 Q2）
> 
> **方案 3：** 使用 `@biomejs/plugin-vue`（社区插件，实验性）

### 6.4 Vite 配置

```typescript
// tooling/vite/src/config.ts
import { defineConfig } from 'vite';
import vue from '@vitejs/plugin-vue';
import vueJsx from '@vitejs/plugin-vue-jsx';

export function createViteConfig(options: ViteConfigOptions) {
  return defineConfig({
    plugins: [
      vue(),
      vueJsx(),
      // 自动导入
      autoImport({
        imports: ['vue', 'vue-router', 'pinia'],
      }),
      // 组件自动注册
      components({
        dirs: ['src/components'],
      }),
    ],
    resolve: {
      alias: {
        '@': '/src',
        '@core': '../../packages/@core/src',
      },
    },
    server: {
      port: options.port || 5173,
      proxy: options.proxy,
    },
    build: {
      target: 'es2015',
      rollupOptions: {
        output: {
          manualChunks: {
            vue: ['vue', 'vue-router', 'pinia'],
            ui: ['element-plus', 'ant-design-vue'],
          },
        },
      },
    },
  });
}
```

### 6.5 Git Hooks 配置（使用 Biome）

```yaml
# lefthook.yml
pre-commit:
  parallel: true
  commands:
    # Biome 检查和格式化（处理 JS/TS/JSON/CSS）
    biome:
      glob: '*.{js,ts,jsx,tsx,json,css}'
      run: biome check --write --no-errors-on-unmatched --files-ignore-unknown=true {staged_files}
    
    # Vue 文件检查（使用轻量级 ESLint）
    vue-lint:
      glob: '*.vue'
      run: eslint --fix {staged_files}
    
    # 类型检查
    typecheck:
      run: pnpm typecheck

commit-msg:
  commands:
    # 简化的 commit 消息检查（不需要 commitlint）
    commit-format:
      run: |
        commit_msg=$(cat {1})
        if ! echo "$commit_msg" | grep -qE '^(feat|fix|docs|style|refactor|perf|test|chore)(\(.+\))?: .{1,}$'; then
          echo "❌ Commit 消息格式错误！"
          echo "正确格式: <type>(<scope>): <subject>"
          echo "类型: feat, fix, docs, style, refactor, perf, test, chore"
          exit 1
        fi

post-merge:
  commands:
    install:
      run: pnpm install
```

#### 简化的 package.json 脚本

```json
{
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "typecheck": "vue-tsc --noEmit",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "lefthook": "^2.0.16",
    "vue-tsc": "^3.2.4"
  }
}
```

#### 性能对比

| 操作 | ESLint + Prettier | Biome | 提升 |
|------|-------------------|-------|------|
| **检查 1000 个文件** | ~15s | ~0.5s | **30x** |
| **格式化 1000 个文件** | ~8s | ~0.3s | **27x** |
| **修复问题** | ~12s | ~0.6s | **20x** |
| **冷启动** | ~2s | ~0.1s | **20x** |

---

## 七、开发规范

### 7.1 命名规范

#### 文件命名

```
✅ 推荐：
- components/UserProfile.vue      # 组件使用 PascalCase
- composables/use-auth.ts         # Composable 使用 kebab-case
- utils/format-date.ts            # 工具函数使用 kebab-case
- types/user.ts                   # 类型文件使用 kebab-case

❌ 避免：
- components/user-profile.vue     # 组件不用 kebab-case
- composables/useAuth.ts          # Composable 不用 camelCase
```

#### 变量命名

```typescript
// 常量使用 UPPER_SNAKE_CASE
export const API_BASE_URL = 'https://api.example.com';
export const MAX_RETRY_COUNT = 3;

// 变量和函数使用 camelCase
const userName = 'John';
function getUserInfo() {}

// 类和接口使用 PascalCase
class UserService {}
interface UserInfo {}

// 类型别名使用 PascalCase
type UserId = string;

// 枚举使用 PascalCase
enum UserRole {
  Admin = 'admin',
  User = 'user',
}
```

### 7.2 目录组织规范

#### 应用层目录结构（推荐）

采用**按技术层级组织**的方式：

```
src/
├── adapter/                     # UI 适配器层
│   ├── component/               # 组件适配器
│   │   └── index.ts
│   └── form.ts                  # 表单适配器
├── api/                         # API 接口层
│   ├── core/                    # 核心 API
│   │   ├── auth.ts              # 认证相关 API
│   │   ├── menu.ts              # 菜单相关 API
│   │   ├── user.ts              # 用户相关 API
│   │   └── index.ts
│   ├── modules/                 # 业务模块 API（可选）
│   │   ├── product.ts
│   │   ├── order.ts
│   │   └── customer.ts
│   ├── request.ts               # 请求客户端配置
│   └── index.ts
├── layouts/                     # 布局组件
│   ├── default.vue              # 默认布局
│   ├── auth.vue                 # 认证页面布局
│   └── blank.vue                # 空白布局
├── locales/                     # 国际化
│   ├── langs/
│   │   ├── zh-CN/
│   │   │   ├── common.json
│   │   │   └── menu.json
│   │   └── en-US/
│   │       ├── common.json
│   │       └── menu.json
│   └── index.ts
├── router/                      # 路由配置
│   ├── routes/
│   │   ├── modules/             # 路由模块
│   │   │   ├── dashboard.ts
│   │   │   ├── system.ts
│   │   │   └── user.ts
│   │   ├── core.ts              # 核心路由（登录、404等）
│   │   └── index.ts
│   ├── guards.ts                # 路由守卫
│   └── index.ts
├── store/                       # 应用级状态管理
│   ├── auth.ts                  # 认证 Store
│   └── index.ts
├── views/                       # 页面视图
│   ├── _core/                   # 核心页面（带下划线前缀）
│   │   ├── authentication/      # 认证相关
│   │   │   ├── login.vue
│   │   │   ├── register.vue
│   │   │   └── forgot-password.vue
│   │   ├── profile/             # 个人中心
│   │   ├── about/               # 关于页面
│   │   └── fallback/            # 错误页面
│   │       ├── 403.vue
│   │       ├── 404.vue
│   │       └── 500.vue
│   ├── dashboard/               # 仪表盘
│   │   ├── analytics.vue
│   │   └── workspace.vue
│   ├── system/                  # 系统管理
│   │   ├── user/
│   │   │   ├── index.vue
│   │   │   ├── components/      # 页面级组件
│   │   │   │   ├── UserForm.vue
│   │   │   │   └── UserTable.vue
│   │   │   └── types.ts         # 页面类型
│   │   ├── role/
│   │   └── menu/
│   └── business/                # 业务模块
│       ├── product/
│       ├── order/
│       └── customer/
├── components/                  # 全局共享组件（可选）
│   ├── common/
│   └── business/
├── App.vue
├── main.ts
├── bootstrap.ts                 # 应用启动配置
├── preferences.ts               # 应用偏好设置
└── env.d.ts
```

#### 核心设计原则

**✅ 优势：**
1. **清晰的分层**：每个目录代表一个技术层级（API、路由、视图）
2. **易于定位**：需要修改路由就去 `router/`，修改 API 就去 `api/`
3. **符合直觉**：符合大多数开发者的思维习惯
4. **易于维护**：相同类型的文件放在一起，便于批量操作

**关键规范：**

1. **API 层组织**
   ```
   api/
   ├── core/           # 核心业务 API（认证、用户、菜单等）
   ├── modules/        # 按业务模块划分的 API（可选）
   ├── request.ts      # 请求客户端配置
   └── index.ts        # 统一导出
   ```

2. **路由层组织**
   ```
   router/
   ├── routes/
   │   ├── modules/    # 按功能模块拆分路由
   │   ├── core.ts     # 核心路由（登录、404等）
   │   └── index.ts
   ├── guards.ts       # 路由守卫
   └── index.ts
   ```

3. **视图层组织**
   ```
   views/
   ├── _core/          # 核心页面（带 _ 前缀，表示系统级）
   ├── dashboard/      # 仪表盘
   ├── system/         # 系统管理
   └── business/       # 业务模块
   
   # 每个页面目录下：
   module/
   ├── index.vue       # 主页面
   ├── components/     # 页面级组件
   ├── composables/    # 页面级 Composables（可选）
   └── types.ts        # 页面类型（可选）
   ```

4. **命名约定**
   - 核心/系统级目录使用 `_` 前缀（如 `_core/`）
   - 业务模块使用普通命名
   - 页面文件使用 `index.vue` 或描述性名称

#### 与"按功能模块组织"的对比

```
❌ 按功能模块（不推荐）：
src/
├── features/
│   └── user/
│       ├── api/
│       ├── components/
│       ├── views/
│       └── routes/

✅ 按技术层级（推荐）：
src/
├── api/
├── components/
├── views/
└── router/
```

**选择按技术层级的原因：**
1. 前端开发通常按"页面 → 组件 → API → 路由"的顺序思考
2. 易于全局搜索和重构
3. 避免跨模块的依赖复杂度
4. 符合 Vue 生态主流实践

### 7.3 组件编写规范

#### 组件结构顺序

```vue
<script setup lang="ts">
// 1. 类型导入
import type { PropType } from 'vue';

// 2. 依赖导入
import { ref, computed, watch } from 'vue';
import { useRouter } from 'vue-router';

// 3. Props 定义
const props = defineProps({
  title: String,
  data: Object as PropType<UserData>,
});

// 4. Emits 定义
const emit = defineEmits<{
  change: [value: string];
  submit: [data: FormData];
}>();

// 5. 响应式数据
const count = ref(0);

// 6. 计算属性
const doubleCount = computed(() => count.value * 2);

// 7. 方法
function increment() {
  count.value++;
}

// 8. 生命周期
onMounted(() => {
  // 初始化逻辑
});

// 9. Watch
watch(() => props.data, (newVal) => {
  // 处理逻辑
});
</script>

<template>
  <div class="component-root">
    <!-- 模板内容 -->
  </div>
</template>

<style scoped>
.component-root {
  /* 样式 */
}
</style>
```

### 7.4 API 调用规范

```typescript
// api/modules/user.ts
import { http } from '@/api/request';
import type { UserInfo, UserListParams, UserListResult } from './types';

// API 接口定义
export const userApi = {
  // 获取用户列表
  getList(params: UserListParams) {
    return http.get<UserListResult>('/users', { params });
  },
  
  // 获取用户详情
  getDetail(id: string) {
    return http.get<UserInfo>(`/users/${id}`);
  },
  
  // 创建用户
  create(data: Partial<UserInfo>) {
    return http.post<UserInfo>('/users', data);
  },
  
  // 更新用户
  update(id: string, data: Partial<UserInfo>) {
    return http.put<UserInfo>(`/users/${id}`, data);
  },
  
  // 删除用户
  delete(id: string) {
    return http.delete(`/users/${id}`);
  },
};
```

### 7.5 Commit 规范

```bash
# 提交格式
<type>(<scope>): <subject>

# Type 类型
feat:     新功能
fix:      Bug 修复
docs:     文档更新
style:    代码格式（不影响代码运行）
refactor: 重构
perf:     性能优化
test:     测试
chore:    构建过程或辅助工具的变动

# 示例
feat(user): 添加用户列表页面
fix(auth): 修复登录 Token 过期问题
docs(readme): 更新安装文档
refactor(http): 重构 HTTP 请求客户端
```

> 💡 **简化 Commit 检查**：
> 不再需要 `commitlint` 和复杂配置，直接在 Git hooks 中使用简单的正则表达式验证即可。

---

## 八、使用示例

### 8.1 创建新应用

```bash
# 1. 安装依赖
pnpm install

# 2. 创建应用
cd apps
cp -r web-antd my-app

# 3. 配置应用
cd my-app
# 修改 package.json 中的应用名称

# 4. 安装 UI 库
pnpm add element-plus

# 5. 创建适配器
# apps/my-app/src/adapter/components.ts

# 6. 启动开发
pnpm dev
```

### 8.2 使用核心包

```typescript
// main.ts
import { createApp } from 'vue';
import { createPinia } from 'pinia';
import { createRouter } from 'vue-router';

// 导入核心包
import '@core/styles';
import { 
  setupI18n,
  setupTheme,
  createAccessGuard,
} from '@core/runtime';

const app = createApp(App);

// 配置状态管理
app.use(createPinia());

// 配置路由
const router = createRouter({...});
app.use(router);

// 配置权限守卫
createAccessGuard(router, {
  mode: 'backend',
  loginPath: '/login',
});

// 配置国际化
setupI18n(app, {
  locale: 'zh-CN',
  fallbackLocale: 'en-US',
});

// 配置主题
setupTheme({
  theme: 'light',
  primaryColor: '#1890ff',
});

app.mount('#app');
```

### 8.3 创建页面

```vue
<!-- views/user/list.vue -->
<script setup lang="ts">
import { useTable } from '@core/composables';
import { userApi } from '@/api/modules/user';

// 使用表格 Hook
const { 
  data, 
  loading, 
  pagination, 
  load, 
  reload 
} = useTable({
  api: userApi.getList,
  immediate: true,
});

// 删除用户
async function handleDelete(id: string) {
  await userApi.delete(id);
  reload();
}
</script>

<template>
  <div class="user-list">
    <VbenTable
      :data="data"
      :loading="loading"
      :pagination="pagination"
      @change="load"
    >
      <VbenTableColumn prop="name" label="姓名" />
      <VbenTableColumn prop="email" label="邮箱" />
      <VbenTableColumn label="操作">
        <template #default="{ row }">
          <button @click="handleDelete(row.id)">删除</button>
        </template>
      </VbenTableColumn>
    </VbenTable>
  </div>
</template>
```

### 8.4 创建插件

```typescript
// packages/plugins/custom-plugin/src/index.ts
import type { App } from 'vue';

export interface CustomPluginOptions {
  apiKey: string;
  debug?: boolean;
}

export function createCustomPlugin(options: CustomPluginOptions) {
  return {
    install(app: App) {
      // 插件逻辑
      app.config.globalProperties.$customPlugin = {
        // ...
      };
    },
  };
}

// 使用插件
import { createCustomPlugin } from '@plugins/custom-plugin';

app.use(createCustomPlugin({
  apiKey: 'your-api-key',
  debug: true,
}));
```

---

## 九、性能优化策略

### 9.1 构建优化

```typescript
// vite.config.ts
export default defineConfig({
  build: {
    // 代码分割
    rollupOptions: {
      output: {
        manualChunks: {
          'vue-vendor': ['vue', 'vue-router', 'pinia'],
          'ui-vendor': ['element-plus'],
          'utils': ['lodash-es', 'dayjs'],
        },
      },
    },
    
    // 压缩
    minify: 'terser',
    terserOptions: {
      compress: {
        drop_console: true,
        drop_debugger: true,
      },
    },
    
    // 资源内联阈值
    assetsInlineLimit: 4096,
  },
  
  // 依赖预构建
  optimizeDeps: {
    include: [
      'vue',
      'vue-router',
      'pinia',
      'axios',
    ],
  },
});
```

### 9.2 路由懒加载

```typescript
// router/routes.ts
const routes = [
  {
    path: '/user',
    component: () => import('@/views/user/index.vue'),
    children: [
      {
        path: 'list',
        component: () => import('@/views/user/list.vue'),
      },
      {
        path: 'detail/:id',
        component: () => import('@/views/user/detail.vue'),
      },
    ],
  },
];
```

### 9.3 组件懒加载

```vue
<script setup lang="ts">
import { defineAsyncComponent } from 'vue';

// 异步组件
const HeavyComponent = defineAsyncComponent({
  loader: () => import('./HeavyComponent.vue'),
  loadingComponent: LoadingSpinner,
  delay: 200,
  timeout: 3000,
});
</script>
```

### 9.4 虚拟列表

```vue
<script setup lang="ts">
import { VirtualList } from '@core/ui/virtual-list';

const items = ref(Array.from({ length: 10000 }, (_, i) => ({
  id: i,
  name: `Item ${i}`,
})));
</script>

<template>
  <VirtualList
    :items="items"
    :item-height="50"
    :buffer="5"
  >
    <template #default="{ item }">
      <div>{{ item.name }}</div>
    </template>
  </VirtualList>
</template>
```

### 9.5 请求优化

```typescript
// 请求去重
import { dedupeRequests } from '@core/runtime/request';

const http = createHttpClient({
  // 启用请求去重
  dedupe: true,
});

// 请求缓存
const http = createHttpClient({
  // 启用缓存
  cache: {
    enable: true,
    maxAge: 5 * 60 * 1000, // 5 分钟
  },
});

// 并发控制
import { ConcurrencyManager } from '@core/runtime/request';

const manager = new ConcurrencyManager({ limit: 6 });
const requests = urls.map(url => 
  manager.add(() => http.get(url))
);
await Promise.all(requests);
```

---

## 十、最佳实践

### 10.1 状态管理最佳实践

```typescript
// ✅ 推荐：按模块拆分 Store
// stores/modules/user.ts
export const useUserStore = defineStore('user', () => {
  const userInfo = ref<UserInfo | null>(null);
  
  async function fetchUserInfo() {
    const data = await userApi.getInfo();
    userInfo.value = data;
  }
  
  return {
    userInfo: readonly(userInfo),
    fetchUserInfo,
  };
});

// ❌ 避免：所有状态放在一个 Store
export const useGlobalStore = defineStore('global', () => {
  const user = ref(null);
  const product = ref(null);
  const order = ref(null);
  // ...
});
```

### 10.2 组件通信最佳实践

```typescript
// ✅ 推荐：使用 Props 和 Emits
<script setup lang="ts">
const props = defineProps<{
  value: string;
}>();

const emit = defineEmits<{
  update: [value: string];
}>();
</script>

// ✅ 推荐：跨层级使用 Provide/Inject
// 父组件
provide('theme', theme);

// 子孙组件
const theme = inject<Theme>('theme');

// ❌ 避免：滥用全局状态
// 简单的父子通信不要用 Store
```

### 10.3 TypeScript 最佳实践

```typescript
// ✅ 推荐：使用类型断言而非 any
const data = response.data as UserInfo;

// ✅ 推荐：使用泛型
function getData<T>(key: string): T {
  return JSON.parse(localStorage.getItem(key)!) as T;
}

// ✅ 推荐：使用联合类型
type Status = 'pending' | 'success' | 'error';

// ✅ 推荐：使用接口组合
interface Timestamps {
  createdAt: Date;
  updatedAt: Date;
}

interface User extends Timestamps {
  id: string;
  name: string;
}

// ❌ 避免：使用 any
function processData(data: any) { // ❌
  // ...
}
```

### 10.4 样式最佳实践

```vue
<style scoped>
/* ✅ 推荐：使用 Tailwind CSS 类名 */
.container {
  @apply flex items-center justify-between px-4 py-2;
}

/* ✅ 推荐：使用 CSS 变量 */
.button {
  background-color: var(--color-primary);
  color: var(--color-text);
}

/* ✅ 推荐：使用 BEM 命名 */
.user-card {}
.user-card__header {}
.user-card__body {}
.user-card--active {}

/* ❌ 避免：深度选择器滥用 */
:deep(.some-class) {
  /* 尽量避免 */
}
</style>
```

### 10.5 错误处理最佳实践

```typescript
// ✅ 推荐：统一错误处理
try {
  const data = await userApi.getList();
  // 处理数据
} catch (error) {
  if (error instanceof ApiError) {
    // 处理 API 错误
    message.error(error.message);
  } else {
    // 处理其他错误
    console.error(error);
  }
}

// ✅ 推荐：使用错误边界
import { ErrorBoundary } from '@core/ui/error-boundary';

<ErrorBoundary
  onError={(error) => console.error(error)}
  fallback={(error) => <ErrorPage error={error} />}
>
  <YourComponent />
</ErrorBoundary>
```

---

## 附录

### A. 常用命令

```bash
# 安装依赖
pnpm install

# 开发
pnpm dev                  # 交互式选择应用
pnpm dev:antd             # 启动 Ant Design 版本
pnpm dev:element          # 启动 Element Plus 版本

# 构建
pnpm build                # 构建所有应用
pnpm build:antd           # 构建指定应用

# 测试
pnpm test                 # 运行测试
pnpm test:unit            # 单元测试
pnpm test:e2e             # E2E 测试

# 代码检查（使用 Biome）
pnpm lint                 # 检查代码（Biome check）
pnpm lint:fix             # 检查并自动修复
pnpm format               # 格式化代码（Biome format）
pnpm typecheck            # 类型检查

# Biome 特定命令
pnpm biome check          # 检查（不修复）
pnpm biome check --write  # 检查并修复
pnpm biome format         # 仅格式化
pnpm biome lint           # 仅 lint
pnpm biome migrate eslint # 从 ESLint 配置迁移

# 发布
pnpm changeset            # 创建 changeset
pnpm version              # 更新版本
pnpm release              # 发布包
```

### B. 核心包 API 索引

```typescript
// @core/base
export { formatDate, isValidEmail, debounce } from '@core/base/utils';
export type { UserInfo, ApiResponse } from '@core/base/types';
export { API_BASE_URL, CACHE_KEYS } from '@core/base/constants';

// @core/ui
export { VbenForm, VbenTable, VbenModal } from '@core/ui';
export { useForm, useTable, useModal } from '@core/ui/hooks';

// @core/runtime
export { createHttpClient, createAccessGuard } from '@core/runtime';
export { useAuth, usePermission, useTabs } from '@core/runtime/composables';
export { useUserStore, useAppStore } from '@core/runtime/store';
```

### C. 性能指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 首屏加载时间 | < 2s | 首次内容绘制 |
| 首次交互时间 | < 3s | 可交互时间 |
| 路由切换 | < 300ms | 路由切换完成时间 |
| 包体积 | < 500KB | Gzip 后的 JS 大小 |
| 构建时间 | < 30s | 完整构建时间 |
| 开发启动 | < 5s | 开发服务器启动时间 |

### D. 浏览器支持

| 浏览器 | 最低版本 |
|--------|---------|
| Chrome | 90+ |
| Firefox | 88+ |
| Safari | 14+ |
| Edge | 90+ |

---

## 总结

本架构方案的核心优势：

1. **极简设计**：整洁架构设计原则
2. **清晰分层**：核心 + 插件，职责明确
3. **UI 无关**：通过适配器支持多种 UI 库
4. **高性能**：构建优化、懒加载、虚拟列表等
5. **工程化**：完整的工具链和规范
6. **易维护**：单一职责、类型安全、测试覆盖
7. **易扩展**：插件化设计、组合式 API

这是一个**可落地、可维护、可扩展**的企业级前端架构方案！🎉

---
