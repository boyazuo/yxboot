import type { ThemeMode } from '../../base/types/app';

export const presetThemes: Record<ThemeMode, { primaryColor?: string }> = {
  light: { primaryColor: '#1890ff' },
  dark: { primaryColor: '#1890ff' },
  auto: { primaryColor: '#1890ff' },
};
